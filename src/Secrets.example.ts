// Copy this file to `src/Secrets.ts` (gitignored) and fill in your key.
//
// Get a key at https://openrouter.ai/keys — a free account is enough for the
// `:free` models the chat panel uses.
//
// Note: this key ships in the browser bundle. That is fine for local and
// internal use, but anyone with access to a deployed build can read it. Keep
// the account on free models only, or move the call server-side (the kiosk
// fallback in functions/ already does this) before exposing it publicly.
export const OPENROUTER_API_KEY = "";
